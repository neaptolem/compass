import { Component } from '@angular/core';

import { templatePath } from '../../utils';

@Component({
  selector: 'compass-app',
  templateUrl: templatePath('screens/app/app.component.html')
})
export default class AppComponent {}
